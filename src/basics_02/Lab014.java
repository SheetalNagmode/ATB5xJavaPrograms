package src.basics_02;

public class Lab014 {
    public static void main(String[] args) {

        int enum1 = 9;
        System.out.println(enum1);

     // int enum = 9; // Can't assign keyword as a variable name
        int Enum = 9; // Upper case keyword is accepted

    //  byte b = 128; // out of range

    //  int a,b,c = 1,2,3;  // wrong way
        int a = 1, b = 2, c = 3;  // correct way

    //  boolean b1 = TRUE // should be in lower case
        boolean b1 = true;
        boolean b2 = false;
    //  boolean b3 = 123; // No numbers. Only two values → true and false.


    //  Literals → value of a variable is literal
    //  can be numeric or non numeric
    //  Integer literal → whole numbers without decimal. e.g : 42 or -123
    //  Floating point literal → numbers with decimal point. e.g : 3.14 or -0.0025
    //  Character literal → a single character enclosed in single quote. e.g: 'a'
    //  Boolean literal → a value of either true or false
    //  String literal → a sequence of characters enclosed in double quotes. e.g: "Hello world"
    //  Null literals → a special literal that represents null reference or a null value


    //  int sheetal = null;
        String name = null; // null only assigned to non - primitive - reference type







    }
}
